# Example .env file for tic-tac-db configuration

# Optional: Specify the path to the SQLite database file.
# If commented out or not provided, a temporary file will be created in the OS temp directory.
# DATABASE_PATH="./tic-tac-toe.db"
